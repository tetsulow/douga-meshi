<h2>ユーザ情報編集</h2>
<%= form_with model:@user, local:true do |f| %>

<%= f.label :姓 %>
<%= f.text_field :last_name %>

<%= f.label :名 %>
<%= f.text_field :first_name %>

<%= f.label :メールアドレス %>
<%= f.text_field :email %>

<%= f.submit "編集内容を保存" %>

<% end %>
